# 运行感知系统必需（含推理）：装 ultralytics 时 torch/torchvision 会自动带上
ultralytics>=8.0.0
opencv-python>=4.5.0
numpy>=1.20.0
pypylon
empy==3.3.4
# 以下为训练附带依赖（ultralytics 自动安装，无需手动操作）
# torch>=2.0.0
# torchvision>=0.15.0
